# Copy to .env and fill in values. For Docker (docker compose up), use postgresql+asyncpg.
# Database (PostgreSQL). Use postgresql+asyncpg:// for async SQLAlchemy (required in Docker).
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/fastapi_beyond_crud
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fastapi_beyond_crud

JWT_SECRET=
JWT_ALGORITHM=HS256

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SERVER=
MAIL_PORT=
MAIL_FROM=
MAIL_FROM_NAME=

DOMAIN=http://localhost:8000
